version: "3.9"
services:
  app:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - .:/workspace
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    depends_on: []
